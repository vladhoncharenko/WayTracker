'use strict';

describe('wayTracker.map module', function () {

    beforeEach(module('wayTracker.map'));

    describe('map controller', function () {

        it('should ....', inject(function ($controller) {
            let view1Ctrl = $controller('MapCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});